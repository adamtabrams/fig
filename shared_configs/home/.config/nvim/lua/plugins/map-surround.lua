-- stylua: ignore
-- if true then return {} end

return {
  "echasnovski/mini.surround",
  opts = {
    mappings = {
      add = "ys",
      delete = "ds",
      replace = "cs",
      -- find = "gzf",
      -- find_left = "gzF",
      -- highlight = "gzh",
      -- update_n_lines = "gzn",
      -- suffix_last = "gzl",
      -- suffix_next = "gzn",
    },
  },
}
