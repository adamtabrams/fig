-- stylua: ignore
-- if true then return {} end

return {
  "rcarriga/nvim-notify",
  opts = {
    stages = "static",
    render = "wrapped-compact",
    -- max_width = 200,
    -- timeout = 5000,
  },
}
